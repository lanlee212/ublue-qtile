[Service]
# ublue's default runs `bootc upgrade --apply`, which deploys AND
# reboots, ignoring /etc/bootc/update-strategy.conf ("pull" = stage
# only). Replace with plain `bootc upgrade` so the strategy governs:
# weekly background fetch + stage, manual reboot to apply.
ExecStart=
ExecStart=/usr/bin/bootc upgrade --quiet
