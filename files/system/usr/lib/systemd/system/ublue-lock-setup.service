[Unit]
Description=Enable the per-user suspend lock (betterlockscreen@<user>)
ConditionPathExists=!/etc/.ublue-lock-setup
After=systemd-user-sessions.service

[Service]
Type=oneshot
ExecStart=/usr/libexec/ublue-lock-setup.sh
RemainAfterExit=no

[Install]
WantedBy=multi-user.target
